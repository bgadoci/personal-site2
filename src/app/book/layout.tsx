import { ReactNode } from 'react';

export const metadata = {
  title: 'SHAIPE - Brandon Gadoci',
  description: 'A guide to creating superhuman AI-powered employees through AI Operations in the enterprise.',
};

export default function BookLayout({ children }: { children: ReactNode }) {
  return children;
}
